# Add a provider key to use a hosted model. Leave both values blank to run in Mock Mode.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Comma-separated browser origins allowed to call the API.
CORS_ORIGINS=http://localhost:5173